= semantic_form_for @selection do |f|
  = f.inputs t('.fieldset') do
    = f.input :character_id, :as => :hidden
    = f.input :search
  = f.buttons
= javascript_tag do
  jQuery(window.autocomplete());
  $('#selection_search').focus();
